//https://www.codewars.com/kata/5875b200d520904a04000003/train/javascript

I. function enough(cap, on, wait) {
  if (on + wait <= cap){
    return 0;
  } else {
    return Math.abs(cap - on - wait)
  }
}

II. const enough = (cap, on, wait) => (on + wait) <= cap ? 0 : Math.abs(cap - on - wait);
